// Importing the MainContainer component from the specified path
import { MainContainer } from './components/containers/MainContainer.jsx';
// Importing the external CSS file for styling
import './App.css';

// Defining the App component
export const App = () => {
  // Rendering the MainContainer component
  return (
    <MainContainer />
  );
};